package agora.postman.assertion.testScript.nestingLevelTree;

import java.util.Arrays;

/**
 * @author Juan C. Alonso
 */
public class Main {

    public static void main(String[] args) {

//        // Create a tree with a root node
//        Tree<String> forest = new Tree<String>("root");
//
//        // Create the variable of type tree that we will iterate on
//        Tree<String> current = forest;
//
//        // Given a list of tree paths, this for loop creates the complete tree
//        for (String path : Arrays.asList("200", "200&data", "200&data&tags", "200&data&categories", "200&data&categories&subcategories", "200&data&categories&icon&sizes", "200&data&categories&pictures&sizes", "200&data&pictures&sizes", "200&data&spatial&director_timeline", "200&data&user&skills", "200&data&user&websites", "200&data&user&pictures&sizes", "200&data&uploader&pictures&sizes")) {
//            // Create a variable to store the root
//            Tree<String> root = current;
//
//            // For each item of the hierarchy
//            for (String data : path.split("&")) {
//                // Dive into the tree following the hierarchy by updating the value of current
//                // If the node does not exist, it is added
//                current = current.child(data);
//            }
//
//            // Set current to the root value again
//            current = root;
//        }
//
//        forest.accept(new PrintIndentedVisitor(0));
//
    }

}
